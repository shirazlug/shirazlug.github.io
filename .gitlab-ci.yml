# This file is a template, and might need editing before it works on your project.
# Full project: https://gitlab.com/pages/hugo
image: publysher/hugo

before_script:
  - hugo version

pages:
  script:
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
  
test:
  script:
  - hugo
  except:
  - master



# image: alpine:3.4

# before_script:
#   - apk update && apk add openssl
#   - wget https://github.com/gohugoio/hugo/releases/download/v0.30.2/hugo_0.30.2_Linux-64bit.tar.gz
#   # - echo "3fc43b23a35dfe5290196bd0fc01f13f hugo_0.19_Linux-64bit.tar.gz" | md5sum -c
#   - tar xf hugo_0.30.2_Linux-64bit.tar.gz && cp ./hugo /usr/bin/hugo
#   - hugo version

# test:
#   script:
#   - hugo
#   except:
#   - master

# pages:
#   script:
#   - hugo
#   artifacts:
#     paths:
#     - public
#   only:
#   - master
